# ========================================
# API Base URL
# ========================================
# Leave empty for same-origin requests (when using proxy)
# Or specify the backend URL:
# VITE_API_BASE_URL=http://localhost:8080

# For production, this can be set at build time or left empty
# if the backend serves the frontend
VITE_API_BASE_URL=
